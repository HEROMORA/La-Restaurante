package restaurant.models.order;

// This enum represents the status of the order
// if it's waiting or the chef is making it or it has been already served

public enum OrderStatus {
    WAITING,
    ONGOING,
    FINISHED
}
